# CatchCore 节点部署系统总结

**更新时间**: 2025-11-14
**版本**: 0.1.0

---

## 📋 概览

CatchCore 现已实现完整的分布式节点部署系统。通过本文档创建的基础设施，您可以轻松部署和管理多个扫描节点，实现真正的分布式漏洞扫描能力。

### 核心特性

✅ **自动节点注册** - 节点自动注册到 Master 服务器
✅ **心跳监控** - 实时监控节点健康状态
✅ **智能任务分配** - 根据节点容量和类型自动分配任务
✅ **多节点类型** - 支持 Scanner、Worker、Coordinator 三种类型
✅ **Docker 支持** - 一键部署容器化节点
✅ **性能监控** - 实时监控 CPU、内存、磁盘使用率
✅ **故障转移** - 自动检测离线节点并重新分配任务

---

## 🏗️ 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│           Master 服务器 (中央控制器)                      │
│                                                         │
│  ┌──────────────┬──────────────┬──────────────┐        │
│  │  FastAPI     │  PostgreSQL  │    Redis     │        │
│  │  后端服务    │   数据库     │   任务队列    │        │
│  └──────────────┴──────────────┴──────────────┘        │
│                                                         │
│  ┌─────────────────────────────────────────────┐       │
│  │  节点管理器 & 任务调度器 & 健康检查        │       │
│  │  - 节点注册与管理                         │       │
│  │  - 任务分配与跟踪                         │       │
│  │  - 心跳监控与离线检测                     │       │
│  │  - 性能指标收集与分析                     │       │
│  └─────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────┘
           ▲          ▲          ▲          ▲
           │          │          │          │
           ▼          ▼          ▼          ▼
    ┌────────────┐┌────────────┐┌────────────┐┌────────────┐
    │ 扫描节点    ││ 扫描节点    ││ Worker节点  ││ Worker节点  │
    │ Node 1     ││ Node 2     ││ Node 3    ││ Node 4    │
    │ Scanner    ││ Scanner    ││ POC/Crack ││ POC/Crack │
    └────────────┘└────────────┘└────────────┘└────────────┘
```

### 关键组件

#### 1. Master 节点
- **角色**: 中央控制与协调
- **功能**:
  - API 服务（FastAPI）
  - 数据库（PostgreSQL）
  - 缓存队列（Redis）
  - 节点管理与监控
  - 任务调度与分配

#### 2. 扫描节点 (Scanner)
- **角色**: 执行网络扫描任务
- **功能**:
  - 端口扫描
  - 服务识别
  - 指纹识别
  - 并发任务: 5-10 个
  - 资源消耗: 中等

#### 3. Worker 节点
- **角色**: 执行计算密集型任务
- **功能**:
  - POC 检测
  - 密码破解
  - 并发任务: 2-5 个
  - 资源消耗: 高

---

## 📁 新增文件清单

### 核心实现文件

| 文件路径 | 说明 | 大小 |
|---------|------|------|
| `backend/app/models/node.py` | Node 数据模型 | 已有 |
| `backend/app/api/v1_nodes.py` | Node API 路由 | 新增 |
| `backend/app/schemas/node.py` | Node 请求/响应 schema | 新增 |
| `backend/app/services/node_service.py` | Node 业务逻辑服务 | 新增 |
| `backend/node_agent.py` | 节点代理程序 | 新增 |

### Docker 相关文件

| 文件路径 | 说明 |
|---------|------|
| `Dockerfile.node` | 节点 Docker 镜像 |
| `docker-compose.nodes.yml` | 多节点编排配置 |
| `node_config_template.yml` | 节点配置模板 |

### 部署与工具

| 文件路径 | 说明 |
|---------|------|
| `setup_nodes.sh` | 节点自动部署脚本 |
| `docs/NODE_DEPLOYMENT_GUIDE.md` | 完整部署指南 |
| `docs/NODE_DEPLOYMENT_SUMMARY.md` | 本文件 |

---

## 🚀 快速开始

### 方式一: 使用自动部署脚本 (推荐)

```bash
# 进入项目根目录
cd /path/to/CatchCore

# 1. 检查系统依赖
./setup_nodes.sh check

# 2. 安装 Python 依赖
./setup_nodes.sh install-deps

# 3. 创建 2 个 Scanner 节点 和 1 个 Worker 节点
./setup_nodes.sh create-multi 2 1

# 4. 启动所有节点
./setup_nodes.sh start-all

# 5. 检查节点状态
./setup_nodes.sh status
```

### 方式二: 使用 Docker Compose

```bash
cd /path/to/CatchCore

# 1. 构建 Docker 镜像
./setup_nodes.sh build-docker

# 2. 启动所有服务和节点
docker-compose -f docker-compose.yml -f docker-compose.nodes.yml up -d

# 3. 查看节点日志
docker-compose logs -f scanner-node-1

# 4. 查看节点状态
curl http://localhost:8000/api/v1/nodes -H "Authorization: Bearer YOUR_TOKEN"
```

### 方式三: 手动部署

详见 [NODE_DEPLOYMENT_GUIDE.md](NODE_DEPLOYMENT_GUIDE.md)

---

## 📊 API 端点总览

### 节点管理 API

```
POST   /api/v1/nodes/register              # 注册新节点
GET    /api/v1/nodes                       # 列出所有节点
GET    /api/v1/nodes/{node_id}             # 获取节点详情
GET    /api/v1/nodes/{node_id}/health      # 检查节点健康状态
PUT    /api/v1/nodes/{node_id}             # 更新节点配置
POST   /api/v1/nodes/{node_id}/heartbeat   # 发送心跳
DELETE /api/v1/nodes/{node_id}             # 删除节点
GET    /api/v1/nodes/stats/summary         # 获取节点统计信息
```

### 使用示例

```bash
# 注册节点
curl -X POST http://localhost:8000/api/v1/nodes/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "scanner-node-01",
    "host": "192.168.1.100",
    "port": 8001,
    "node_type": "scanner",
    "max_concurrent_tasks": 5
  }'

# 列出所有节点
curl -X GET http://localhost:8000/api/v1/nodes \
  -H "Authorization: Bearer YOUR_TOKEN"

# 检查节点健康状态
curl -X GET http://localhost:8000/api/v1/nodes/1/health \
  -H "Authorization: Bearer YOUR_TOKEN"

# 获取节点统计
curl -X GET http://localhost:8000/api/v1/nodes/stats/summary \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 🔧 节点配置

### 最小配置

```yaml
node:
  name: "scanner-node-01"
  type: "scanner"
  host: "192.168.1.100"
  port: 8001

master:
  host: "192.168.1.50"
  port: 8000
  api_url: "http://192.168.1.50:8000/api/v1"

redis:
  host: "192.168.1.50"
  port: 6379
  db: 0

performance:
  max_concurrent_tasks: 5
  heartbeat_interval: 30
  check_task_interval: 5
```

### 完整配置

详见 `node_config_template.yml`

---

## 📈 性能指标

### 节点资源监控

节点会定期向 Master 报告以下指标:

```json
{
  "node_id": 1,
  "name": "scanner-node-01",
  "status": "online",
  "cpu_usage": 45.5,          // 百分比
  "memory_usage": 62.3,       // 百分比
  "disk_usage": 38.9,         // 百分比
  "current_tasks": 3,         // 当前运行任务数
  "max_concurrent_tasks": 5,  // 最大并发任务数
  "last_heartbeat": "2025-11-14T10:30:00Z"
}
```

### 任务分配策略

1. **健康性检查**: 只分配给在线节点
2. **容量检查**: 确保 `current_tasks < max_concurrent_tasks`
3. **负载均衡**: 选择任务最少的节点
4. **节点类型**: 根据任务类型选择合适的节点
5. **地域亲和性**: 优先选择同区域节点 (可配置)

---

## 🔍 监控和维护

### 实时监控

通过 Web UI 查看:
- 节点在线状态
- 资源使用情况
- 任务分布
- 性能指标

访问地址: `http://localhost:3000/nodes`

### 命令行监控

```bash
# 查看所有节点
curl http://localhost:8000/api/v1/nodes | jq

# 查看离线节点
curl http://localhost:8000/api/v1/nodes?status_filter=offline | jq

# 查看特定类型节点
curl http://localhost:8000/api/v1/nodes?node_type=scanner | jq

# 查看节点统计
curl http://localhost:8000/api/v1/nodes/stats/summary | jq
```

### 日志查看

```bash
# 直接运行节点
tail -f logs/node_agent.log

# Systemd 服务
sudo journalctl -u catchcore-node -f

# Docker 容器
docker logs -f catchcore-scanner-1

# Docker Compose
docker-compose logs -f scanner-node-1
```

---

## 🛠️ 常见任务

### 添加新节点

```bash
# 1. 创建配置
./setup_nodes.sh create-single scanner-node-03 scanner 8003

# 2. 安装依赖 (如果是首次部署)
./setup_nodes.sh install-deps

# 3. 启动节点
./setup_nodes.sh start-single scanner-node-03
```

### 扩展节点集群

```bash
# 添加 3 个新的 Scanner 节点和 2 个 Worker 节点
./setup_nodes.sh create-multi 3 2
./setup_nodes.sh start-all
```

### 更新节点配置

```bash
# 编辑配置文件
nano node_configs/scanner-node-01.yml

# 重启节点生效
systemctl restart catchcore-node
# 或 Docker 用户:
docker restart catchcore-scanner-1
```

### 从集群中移除节点

```bash
# 1. 停止节点
systemctl stop catchcore-node
# 或 Docker:
docker stop catchcore-scanner-1

# 2. 删除节点 (通过 API)
curl -X DELETE http://localhost:8000/api/v1/nodes/1 \
  -H "Authorization: Bearer YOUR_TOKEN"

# 3. 清理配置文件
rm node_configs/scanner-node-01.yml
```

---

## ⚠️ 故障排查快速表

| 问题 | 症状 | 解决方案 |
|------|------|--------|
| 节点无法连接 Master | "无法连接到 Master" | 检查网络, 防火墙规则, Master 是否运行 |
| 任务分配失败 | 任务停留在 PENDING | 检查节点是否在线, 容量是否充足 |
| 节点内存泄漏 | 内存持续增长 | 增加垃圾回收频率, 定期重启节点 |
| 任务执行超时 | 任务经常超时 | 增加超时时间, 检查网络连接 |
| Redis 连接错误 | "ConnectionError" | 检查 Redis 是否运行, 内存是否充足 |

详细故障排查见 [NODE_DEPLOYMENT_GUIDE.md](NODE_DEPLOYMENT_GUIDE.md#故障排查)

---

## 📚 相关文档

- [完整部署指南](NODE_DEPLOYMENT_GUIDE.md) - 详细的部署说明和配置
- [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md) - Master 服务器部署
- [TROUBLESHOOTING_GUIDE.md](TROUBLESHOOTING_GUIDE.md) - 常见问题排查
- [BEGINNER_GUIDE.md](BEGINNER_GUIDE.md) - 初学者指南

---

## 🔐 安全建议

✅ **推荐做法**
- 使用 HTTPS 在生产环境
- 为 Redis 设置密码认证
- 限制节点网络访问范围
- 定期更新依赖包
- 监控异常流量和行为
- 备份数据库定期进行

❌ **应避免**
- 在互联网暴露 Redis
- 使用默认密码
- 禁用身份验证
- 过度配置节点任务
- 忽视安全补丁

---

## 📝 版本信息

- **CatchCore 版本**: 0.1.0
- **节点系统版本**: 0.1.0
- **Python**: 3.10+
- **PostgreSQL**: 13+
- **Redis**: 6.0+

---

## 🤝 贡献与反馈

如有问题或改进建议，请:
1. 查看 [故障排查指南](NODE_DEPLOYMENT_GUIDE.md#故障排查)
2. 提交 GitHub Issue
3. 提交 Pull Request

**项目地址**: https://github.com/TaoKe-One/CatchCore

---

## 📅 更新日志

### v0.1.0 (2025-11-14)
- ✅ 实现节点数据模型
- ✅ 创建节点 API 路由
- ✅ 开发节点服务类
- ✅ 实现节点代理程序
- ✅ 创建 Docker 部署方案
- ✅ 编写完整部署文档
- ✅ 开发自动化设置脚本

---

**文档维护**: TaoKe-One
**最后更新**: 2025-11-14

